import React from 'react'


function Logout() {
    const handleSubmit = ()=>{
        
    }

return (
<div>

<button onClick={()=>handleSubmit()} type="submit" class="btn">Logout</button>

</div>
)
}

export default Logout;